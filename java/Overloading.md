## Overloading

- Method overloading allows you to define multiple methods within the same class that share the same name but have different parameter declarations.
- Method Overloading is one of the way that Java supports polymorphism.